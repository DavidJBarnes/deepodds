__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
.env

node_modules/
frontend/dist/
frontend/.vite/

.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

.pi
.opencode

# Runtime model snapshots (versioned by ModelTrainHistory rows).
# The .gitkeep stays so the dir exists; per-retrain v_*.json files are local artifacts.
backend/app/core/models/**/v_*.json

# Platt scaler coefficients — refitted at runtime from settled signals.
backend/app/core/climate_platt.json

# Backtest data cache and results (large binary/CSV, not committed)
backend/backtest/data/
backend/backtest/results/
backend/backtest/REPORT_equity.png

# Kalshi backtest data + output (large CSV, not committed)
backend/kalshi_backtest/data/
